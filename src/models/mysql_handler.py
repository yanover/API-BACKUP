import MySQLdb

class MYSQL:
    def __init__(self, host, port, user, password):
        self.host = host
        self.port = port
        self.user = user
        self.password = password

    def save(self, databases):
        

        return data

    def execute(cmd):
        pass

